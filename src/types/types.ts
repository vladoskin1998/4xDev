export interface MenuLinksInterface {
    label: string,
    link: string
}

export interface ContactsItemInterface {
    id: number | null,
    name: string,
    number: string,
    avatar: string,
}